# Makefile to build Regent programs.

# Define the executable name.
EXEC = sqrt

# Include the script for rules "exec" (default, build the executable" and "clean".
include ../../libs/scripts/build-rules.mk